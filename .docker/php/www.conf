[www]
user = www-data
group = www-data

listen = 9000

; Dynamic pool — scales between min and max based on demand
pm = dynamic
pm.max_children = 20
pm.start_servers = 5
pm.min_spare_servers = 4
pm.max_spare_servers = 10
pm.max_requests = 500
