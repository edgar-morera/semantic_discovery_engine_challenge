services:
  php:
    build:
      context: .
      dockerfile: .docker/php/Dockerfile
    container_name: semantic_php
    volumes:
      - ./web:/var/www/html
    environment:
      HUGGINGFACE_API_KEY: ${HUGGINGFACE_API_KEY}
      QDRANT_DSN: ${QDRANT_DSN}
      DATABASE_URL: ${DATABASE_URL}
      REDIS_DSN: ${REDIS_DSN}
    networks:
      - app_network
    depends_on:
      mysql:
        condition: service_healthy
      qdrant:
        condition: service_started

  nginx:
    image: nginx:1.27-alpine
    container_name: semantic_nginx
    ports:
      - "8080:80"
    volumes:
      - ./web:/var/www/html
      - ./.docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - app_network
    depends_on:
      - php

  mysql:
    image: mysql:8.0
    container_name: semantic_mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "${MYSQL_PORT}:3306"
    volumes:
      - ./.docker/mysql/data:/var/lib/mysql
    networks:
      - app_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:v1.9.0
    container_name: semantic_qdrant
    ports:
      - "${QDRANT_HTTP_PORT}:6333"
      - "${QDRANT_GRPC_PORT}:6334"
    volumes:
      - ./.docker/qdrant/data:/qdrant/storage
    networks:
      - app_network

  redis:
    image: redis:7.2-alpine
    container_name: semantic_redis
    networks:
      - app_network

  worker:
    build:
      context: .
      dockerfile: .docker/php/Dockerfile
    container_name: semantic_worker
    command: ["php", "bin/console", "messenger:consume", "async", "--time-limit=3600", "--memory-limit=128M"]
    volumes:
      - ./web:/var/www/html
    environment:
      HUGGINGFACE_API_KEY: ${HUGGINGFACE_API_KEY}
      QDRANT_DSN: ${QDRANT_DSN}
      DATABASE_URL: ${DATABASE_URL}
      REDIS_DSN: ${REDIS_DSN}
    networks:
      - app_network
    depends_on:
      mysql:
        condition: service_healthy
      qdrant:
        condition: service_started
      redis:
        condition: service_started

  k6:
    image: grafana/k6:latest
    profiles:
      - k6
    volumes:
      - ./k6:/scripts
    networks:
      - app_network
    depends_on:
      - nginx

networks:
  app_network:
    driver: bridge
